# 客户端
## 互动界面
**通过shell类与cmd互动**
1. Shell类通过REPL（Read–Eval–Print Loop）不断接受命令调用Client.execute()方法将结果返回
2. client.excute通过RoundTripper类来**传输包**并返**拿到返回结果**
3. RoundTripper类底层是一个包管理器(Packager)
4. 包管理器(Packager)由Enconder和Transporter组成,负责把传入的package转为byte[]然后send,以及receive来自后端的byte[]转为package
5.  Encoder负责package与byte[]的转换,Transporter通过socket与目标ip+端口建立Tcp连接,并负责将字节数组写入socket
## package类
```java
public class Package {
    byte[] data;
    Exception err;
	// data != null → 正常
	// err != null → 错误
	// 两者不会同时有值
    public Package(byte[] data, Exception err) {
        this.data = data;
        this.err = err;
    }

    public byte[] getData() {
        return data;
    }

    public Exception getErr() {
        return err;
    }
}
```
